apiVersion: v1
items:
  - apiVersion: v1
    kind: Service
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yml -o kube.yml --controller deployment --push-image
        kompose.version: 1.26.0 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.service: website
      name: website
    spec:
      ports:
        - name: '5000'
          port: 5000
          targetPort: 5000
      selector:
        io.kompose.service: website
    status:
      loadBalancer: {}
  - apiVersion: apps/v1
    kind: Deployment
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yml -o kube.yml --controller deployment --push-image
        kompose.version: 1.26.0 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.service: website
      name: website
    spec:
      replicas: 1
      selector:
        matchLabels:
          io.kompose.service: website
      strategy: {}
      template:
        metadata:
          annotations:
            kompose.cmd: kompose convert -f docker-compose.yml -o kube.yml --controller deployment --push-image
            kompose.version: 1.26.0 (HEAD)
          creationTimestamp: null
          labels:
            io.kompose.service: website
        spec:
          containers:
            - image: ghcr.io/midstallsoftware/website:master
              name: website
              ports:
                - containerPort: 5000
              resources: {}
          restartPolicy: Always
    status: {}
kind: List
metadata: {}
